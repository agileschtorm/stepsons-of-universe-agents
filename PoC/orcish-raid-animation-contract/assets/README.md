# Assets

This PoC intentionally does not copy product assets into this repo.

It loads existing assets from sibling SOU repositories through the contract in `src/contract.rs`.

That is deliberate: the goal is to prove module separation and animation binding, not to fork asset ownership.
